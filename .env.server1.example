# ==================== SERVER 1 CONFIGURATION ====================
# Server 1 chạy: API Gateway + User Service + MongoDB Cluster
# Tailscale IP: 100.69.63.99

# MongoDB Configuration
MONGO_URL=mongodb://mongos:27017

# JWT Configuration
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# API Gateway Configuration
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8000

# Tailscale IP của Server 1 (này)
MY_TAILSCALE_IP=100.69.63.99

# Tailscale IPs của các Services khác
VEHICLE_SERVICE_URL=http://100.73.22.88:8002
BOOKING_SERVICE_URL=http://100.65.117.32:8003
PAYMENT_SERVICE_URL=http://100.108.163.69:8004

# ==================== DEPLOYMENT NOTES ====================
# 1. Copy file này thành .env: cp .env.server1.example .env
# 2. Thay đổi SECRET_KEY thành giá trị bảo mật của bạn
# 3. Cập nhật các Tailscale IPs cho đúng với setup của bạn
# 4. Deploy: docker compose -f docker-compose.sharded-hybrid.yml up -d
