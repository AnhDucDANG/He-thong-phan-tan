# ============================================================
# BOOKING SERVICE - DISTRIBUTED CONFIGURATION (Tailscale)
# ============================================================
# File này dùng cho team member chạy booking_service trên máy riêng
# Sao chép file này thành .env khi chạy distributed

# MongoDB Connection (kết nối đến server chính qua Tailscale)
MONGO_URL=mongodb://100.69.63.99:27017
MONGO_DB=rental

# Service Configuration
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8003

# API Gateway URL (QUAN TRỌNG - trỏ đến server chính qua Tailscale)
# Booking service sẽ gọi tất cả service khác qua Gateway này
API_GATEWAY_URL=http://100.69.63.99:8000

# JWT Configuration
SECRET_KEY=35a91c468c0a8a62d3669ba143ddf1db
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Request Timeout
REQUEST_TIMEOUT=120

# ============================================================
# HƯỚNG DẪN:
# 1. Đảm bảo đã cài Tailscale và kết nối vào mạng
# 2. Kiểm tra ping được 100.69.63.99
# 3. Sao chép file này: cp .env.distributed .env
# 4. Chạy: python -m uvicorn app.main:app --host 0.0.0.0 --port 8003
# ============================================================
